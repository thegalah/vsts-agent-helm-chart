# usage

```bash

helm upgrade --install "{SOME_RELEASE_NAME}" "vsts-agent" --namespace "{SOME_NAMESPACE}" --set vstsToken="{SOME_TOKEN}" --set vstsAccount="{SOME_ACCOUNT}" --set vstsPool="{SOME_POOL}"
```
